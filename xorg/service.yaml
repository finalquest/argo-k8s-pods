apiVersion: v1
kind: Service
metadata:
  name: xorg-vnc
  namespace: android-emulators
  labels:
    app: xorg-gpu
spec:
  type: ClusterIP
  selector:
    app: xorg-gpu
  ports:
    - name: vnc
      port: 5900
      targetPort: 5900
