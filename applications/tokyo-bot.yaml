apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tokyo-bot
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/finalquest/argo-k8s-pods
    targetRevision: main
    path: tokyo-bot
  destination:
    server: https://kubernetes.default.svc
    namespace: tokyo-bot
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
