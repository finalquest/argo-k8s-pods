apiVersion: v1
kind: Service
metadata:
  name: pcnt-doc-rag-postgres
  namespace: pcnt-doc-rag
spec:
  type: ClusterIP
  selector:
    app: pcnt-doc-rag-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
