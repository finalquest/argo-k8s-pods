// Test multiline step definition
Given(
  'import mappings wiremock curl {string} with method {string} and body from file {string}',
  async function (url, method, filename) {
    // implementation
  },
);

// Another test
When(
  'user enters username {string} and password {string}',
  async function (username, password) {
    // implementation
  },
);

// Simple one line
Then('the user should see success message', async function () {
  // implementation
});
