# Variables de entorno para Appium Orchestrator

# --- Configuración de Git ---
# Repositorio de donde se clonarán los archivos .feature
GIT_REPO_URL=https://github.com/your-org/your-repo.git
GIT_USER=your-git-username
GIT_PAT=your-personal-access-token

# --- Configuración de Wiremock ---
# URL de la interfaz de administración de Wiremock
WIREMOCK_ADMIN_URL=http://localhost:8081

# --- Configuración del Orquestador ---
# Número máximo de tests (workers) que se ejecutarán en paralelo
MAX_PARALLEL_TESTS=2

# Ruta a la carpeta que contendrá los workspaces de las diferentes branches
PERSISTENT_WORKSPACES_ROOT=./appium_workspaces

# --- Configuración para Desarrollo Local con Docker ---
# Host ADB del emulador corriendo en tu máquina. 
# Usa host.docker.internal para que el contenedor Docker pueda conectarse a tu máquina.
LOCAL_ADB_HOST=host.docker.internal:5555

# --- Variables Legacy (No necesarias si se usa LOCAL_APK_DIRECTORY en docker-compose) ---
# APK_REGISTRY=...
# APK_PATH=...