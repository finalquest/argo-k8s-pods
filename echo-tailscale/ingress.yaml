apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo-ts
  namespace: default
  annotations:
    tailscale.com/tags: '["tag:k8s"]'
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: echo
      port:
        number: 80